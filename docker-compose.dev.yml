version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: finance-mongodb-dev
    ports:
      - "27017:27017"
    volumes:
      - mongo_data_dev:/data/db
    environment:
      - MONGO_INITDB_DATABASE=personal-finance

volumes:
  mongo_data_dev:
